name: Apply ABXY release-block setting

on:
  push:
    branches: [main]
    paths:
      - 'tools/temp-abxy-release-block-setting.py'
      - '.github/workflows/temp-abxy-release-block-setting.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply ABXY release-block setting patch
        run: python tools/temp-abxy-release-block-setting.py
      - name: Run tests
        run: npm test
      - name: Remove temporary files
        run: |
          rm tools/temp-abxy-release-block-setting.py
          rm .github/workflows/temp-abxy-release-block-setting.yml
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Add ABXY release-block setting contract'
          git push
